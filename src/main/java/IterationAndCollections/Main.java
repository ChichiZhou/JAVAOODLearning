package IterationAndCollections;

public class Main {
    public static void main(String[] args) {
        /**
         * Iterator 的用法就是用在 while 循环中
         * iterator.hasnext() 用来进行循环的判断
         * iterator.next() 用来返回下一个元素
         *
         * 凡是实现 collection 接口的，都有一个 iterator
         * 可以用 collection.iterator() 来返回这个 iterator
         *
         */
        MenuItem menuItem = new MenuItem("Ass", "Shilin's ass", 1.2);
    }
}
